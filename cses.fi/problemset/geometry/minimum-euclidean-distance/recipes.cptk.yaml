recipes:
- name: minimum-euclidean-distance
  bake:
  - g++ minimum-euclidean-distance.cpp -DLOCAL -O2 -Wall -o minimum-euclidean-distance
  serve: ./minimum-euclidean-distance
  test:
    folder: tests
    timeout: 1.0
