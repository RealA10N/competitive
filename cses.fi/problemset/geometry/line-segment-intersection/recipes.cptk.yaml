recipes:
- name: line-segment-intersection
  bake:
  - g++ line-segment-intersection.cpp -DLOCAL -O2 -Wall -o line-segment-intersection
  serve: ./line-segment-intersection
  test:
    folder: tests
    timeout: 1.0
